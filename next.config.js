// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // optional, recommended
  // experimental: {
  //   appDir: true, // add this only if you're using the /app directory
  // },
};

module.exports = nextConfig;